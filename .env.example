# Replay accepts either variable name. Keep the value server-side.
OPENAI_API_KEY=
# OPEN_AI_KEY=

# Optional production TxLINE credentials. Local development can read the
# ignored .solana/txline-devnet.json provisioned by the included scripts.
TXLINE_API_TOKEN=
TXLINE_GUEST_JWT=
TXLINE_API_ORIGIN=https://txline-dev.txodds.com
